{
  "name": "githubio-v2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export && touch out/.nojekyll",
    "wexport": "next build && next export && type nul > out/.nojekyll && cd ./out && git init && git remote add github git@github.com:khang8591/khang8591.github.io.git && git add . && git commit -m \"automated commit\" && git push github master -f"
  },
  "dependencies": {
    "next": "9.0.5",
    "node-sass": "^4.12.0",
    "react": "16.9.0",
    "react-dom": "16.9.0"
  }
}
